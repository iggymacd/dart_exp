import 'package:polymer/polymer.dart';

/**
 * A Polymer stack-east-won element.
 */
@CustomTag('stack-east-won')

class StackEastWon extends PolymerElement {

  /// Constructor used to create instance of StackEastWon.
  StackEastWon.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of stack-east-won is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of stack-east-won is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// stack-east-won is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when stack-east-won has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
