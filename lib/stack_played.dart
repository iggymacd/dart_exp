import 'package:polymer/polymer.dart';

/**
 * A Polymer stack-played element.
 */
@CustomTag('stack-played')

class StackPlayed extends PolymerElement {

  /// Constructor used to create instance of StackPlayed.
  StackPlayed.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of stack-played is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of stack-played is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// stack-played is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when stack-played has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
