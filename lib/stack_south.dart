import 'package:polymer/polymer.dart';

/**
 * A Polymer stack-south element.
 */
@CustomTag('stack-south')

class StackSouth extends PolymerElement {

  /// Constructor used to create instance of StackSouth.
  StackSouth.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of stack-south is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of stack-south is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// stack-south is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when stack-south has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
