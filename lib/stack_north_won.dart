import 'package:polymer/polymer.dart';

/**
 * A Polymer stack-north-won element.
 */
@CustomTag('stack-north-won')

class StackNorthWon extends PolymerElement {

  /// Constructor used to create instance of StackNorthWon.
  StackNorthWon.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of stack-north-won is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of stack-north-won is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// stack-north-won is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when stack-north-won has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
