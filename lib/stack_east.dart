import 'package:polymer/polymer.dart';

/**
 * A Polymer stack-east element.
 */
@CustomTag('stack-east')

class StackEast extends PolymerElement {

  /// Constructor used to create instance of StackEast.
  StackEast.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of stack-east is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of stack-east is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// stack-east is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when stack-east has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
