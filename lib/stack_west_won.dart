import 'package:polymer/polymer.dart';

/**
 * A Polymer stack-west-won element.
 */
@CustomTag('stack-west-won')

class StackWestWon extends PolymerElement {

  /// Constructor used to create instance of StackWestWon.
  StackWestWon.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of stack-west-won is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of stack-west-won is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// stack-west-won is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when stack-west-won has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
